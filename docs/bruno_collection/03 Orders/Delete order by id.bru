meta {
  name: Delete order by id
  type: http
  seq: 8
}

delete {
  url: {{orderURL}}/order/{{order_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
