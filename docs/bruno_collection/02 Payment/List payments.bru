meta {
  name: List payments
  type: http
  seq: 2
}

get {
  url: {{paymentURL}}/payment
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
