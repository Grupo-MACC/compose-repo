meta {
  name: Top up wallet
  type: http
  seq: 3
}

put {
  url: {{paymentURL}}/payment/add/{{topUpAmount}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
