meta {
  name: List users (admin)
  type: http
  seq: 6
}

get {
  url: {{authURL}}/user
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{admin_access_token}}
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
